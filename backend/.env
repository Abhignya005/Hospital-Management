# Database Configuration
# Uncomment and set to your MongoDB connection string to use a local/remote DB.
# Example local MongoDB URI:
# MONGO_URI=mongodb://localhost:27017/hospital

# If MONGO_URI is empty or commented out, the app will fall back to an
# in-memory MongoDB instance (useful for local development and testing).

PORT=5001
# Database Configuration

